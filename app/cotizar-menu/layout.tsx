export const metadata = {
  title: "Cotizaciones | QMenú",
  description: "Cotizá el plan ideal para tu menú.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
