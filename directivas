hazme la siguiente logica, necesito tener en mi dashboard una pestaña extra llamada sucursales, donde puedo, crear, editar o eliminar menus, ya tengo creado el modelo y los schemas, al crear un usuario a la vez se crea un menu automaticamente, pero ahora debe poderse una vez creado el usuario crearse mas menus, esto segun el plan que tenga el usuario, los cuales son 3, el primero es el plan gratis, no se puede, el segundo es plan plus se pueden hasta 5, y el plan premium se pueden crear hasta 100 

hazme la siguiente logica, necesito capturar analiticas de usuarios, cual plato es el más pedido, cuantas personas entraron en mi app en los últimos 7 días, para ello simplemente creamos un modelo en mongo donde se capturen esos datos de usuarios que ingresan (numero) y el plato o los platos mas pedidos, (array) uno se guarda en un array y el otro se suma al numero que inicia en 0, todo esto debe estar referenciado a un user_id, el cual se necesita para traer al dashboard del usuario que tiene su cuenta registrada en la app